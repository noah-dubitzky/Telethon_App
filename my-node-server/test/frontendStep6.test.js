const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');const root=path.join(__dirname,'..','public');const read=p=>fs.readFileSync(path.join(root,p),'utf8');
test('all archive pages load the server-session guard',()=>{for(const page of ['desktop/index.html','desktop/sender.html','desktop/channels.html','desktop/filters.html','mobile/index.html','mobile/sender.html','mobile/channels.html','mobile/filters.html','settings.html','manage-account.html'])assert.match(read(page),/session_guard\.js/,`${page} is missing the session guard`)});
test('authentication UI uses only public cookie-session endpoints',()=>{const s=read('scripts/auth.js');for(const e of ['/api/auth/me','/api/auth/login','/api/auth/register'])assert.ok(s.includes(e));assert.doesNotMatch(s,/localStorage|sessionStorage|user_id|TELESAVER_WORKER_SECRET/)});
test('Telegram settings implements phone, code, and password steps without browser storage',()=>{const s=read('scripts/telegram_settings.js');for(const e of ['/api/telegram-accounts','/api/telegram-connect/start','/api/telegram-connect/verify-code','/api/telegram-connect/verify-password'])assert.ok(s.includes(e));assert.doesNotMatch(s,/localStorage|sessionStorage|phone_code_hash|temporary_session|api_hash|worker.secret/i)});
